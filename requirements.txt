viam-sdk>=0.30.0
numpy>=1.24
Pillow>=10.0
typing_extensions>=4.0
# Ultralytics pulls torch (+ often CUDA on Linux aarch64). On a Pi, run.sh
# installs CPU torch/torchvision first; for a manual venv see README.
ultralytics>=8.3
